Purpose: canonical XML schemas and example metadata for Comics Lab.

This folder is the canonical copy for `comics-suite/docs/schemas/`.
Files:
- ComicInfo.xml — canonical ComicInfo example
- MetronInfo.xml — canonical MetronInfo example
- (future) comicinfo.xsd / metroninfo.xsd — validation schemas

Rules:
- Every schema change must include a <MetadataVersion> element.
- Updates require CODEOWNERS review and passing CI validation.
